# Leave empty for same-origin /api (Vite proxy or nginx proxy in Docker).
# Or set the full origin when developing on http://localhost:5173 (CORS-allowed):
# VITE_HEALTH_API_BASE_URL=https://health-api.dev-scorpiusnetworks.com
VITE_HEALTH_API_BASE_URL=
