# Medical Intelligence API origin (no trailing slash).
# Used by Patients, Imaging, and Research Mode pages.
# Leave empty to use the Vite dev proxy at /api → health-api host.
VITE_HEALTH_API_BASE_URL=https://health-api.dev-scorpiusnetworks.com
